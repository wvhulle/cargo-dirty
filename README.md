# cargo-dirty

Find out why Cargo recompiles often.

Installation:

1. Install `rustup`
2. Clone this repo
3. Run `cargo install --path .`

Or without manual cloning, run `cargo install --git [GITHUB_REPO_URL]`
